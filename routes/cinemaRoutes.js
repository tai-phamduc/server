const express = require('express');
const router = express.Router();

// Placeholder route until the full implementation is ready
router.get('/', (req, res) => {
  res.json({ message: 'Cinema routes are under development' });
});

module.exports = router;
